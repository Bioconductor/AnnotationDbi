import(methods)
import(AnnotationDbi)

### Only put what is statically exported here. All the AnnObject instances
### created at load time are dynamically exported (refer to R/zzz.R for more
### details).
export("@OBJPREFIX@ORGANISM")

